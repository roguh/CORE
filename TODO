GMachine mk6
    fix letrec bindings, see program let3.core

incremental output: works ok @ indexing, it must print each element as it is evaluated though...
    let rs = readFile "cores/test_compiler/ultimateTest.core" >>= return . either ((:[]).pack) id . (parse "" >=> compileMk6 >=> evalMk6 >=> return . showIncrementalResults) . pack >>= (putStrLn . unpack . (!!1001))

garbage collector

lambda lifter

GMachine mk7

TIM

parallel GMachine

STG - spineless tagless gmachine
    see Peyton Jones' other paper
